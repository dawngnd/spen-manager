{
  "name": "spen-manager",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "node esbuild.config.js && cp src/appsscript.json dist/appsscript.json",
    "deploy": "npm run build && cd dist && npx @google/clasp push --force",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@types/google-apps-script": "^2.0.11",
    "esbuild": "^0.28.1",
    "esbuild-gas-plugin": "^0.10.0",
    "typescript": "^7.0.2"
  }
}
